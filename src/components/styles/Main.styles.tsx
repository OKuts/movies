import styled from 'styled-components';

export const MainWrapper = styled.main`
    display: block;
    padding: 0 50px;

    .ant-layout-content {
        flex: auto;
        min-height: 0;
    }
`;
